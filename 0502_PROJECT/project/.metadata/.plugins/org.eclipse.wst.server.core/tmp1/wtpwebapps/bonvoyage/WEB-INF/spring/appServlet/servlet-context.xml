<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.3.xsd">

		<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<!-- 컨트롤러가 리턴하는 뷰 파일명을 받아서, /WEB-INF/views/리턴받은 파일명.jsp 
		해당 위치에 있는 파일을 찾아서 클라이언트에게로 내보내는 일을 함
	-->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
		<beans:property name="order" value="1" />
	</beans:bean>
	
	<!-- 파일 다운을 위한 뷰 클래스 실행하는 뷰 리졸버 등록함
		주의 : id 속성을 사용하면 안됨
		여러 개의 뷰 리졸버를 등록할 때는 실행 순서를 정해야 함
		파일 다운용 뷰 리졸버가 반드시 맨 처음 실행되게 순서를 지정함
		컨트롤러가 리턴하는 뷰 이름과 일치하는 뷰 클래스가 있으면, 자동으로 해당 뷰 클래스가 실행됨
		뷰 이름과 일치하는 뷰 클래스가 없으면 다음 순서인 뷰 리졸버로 리턴값이 넘어감
	-->
	<beans:bean
	 	class="org.springframework.web.servlet.view.BeanNameViewResolver">
		<beans:property name="order" value="0" />
		<!-- 제일 먼저 실행되도록 순번 지정함 -->
	</beans:bean>
	
	<!-- ajax 통신으로 컨트롤러가 JsonObject 를 jsonString 으로 변환해서 리턴하면, 작동할 뷰 리졸버를 추가 등록해야 함
		pom.xml 에 dependency 로 추가한 다음 사용해야 함 : Json View
	  -->
	<beans:bean class="net.sf.json.spring.web.servlet.view.JsonView"
		id="jsonView">
	</beans:bean>
	
	<context:component-scan base-package="com.synergysoft.bonvoyage" />
	
	 <beans:bean
	 	class="org.springframework.web.multipart.commons.CommonsMultipartResolver"
	 	id="multipartResolver">
	 <beans:property name="maxUploadSize" value="10000000" />
	 </beans:bean>
	 
	 <aop:aspectj-autoproxy proxy-target-class="true" />
	
</beans:beans>
